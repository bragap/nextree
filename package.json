{
  "name": "nextree",
  "displayName": "Nextree",
  "description": "Visualize Next.js component usage as a tree inside VS Code.",
  "version": "0.0.1",
  "engines": {
    "vscode": ">=1.80.0"
  },
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "contributes": {
    "commands": [
      {
        "command": "nextree.showTree",
        "title": "Show Nextree Component Tree"
      }
    ]
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@types/mocha": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
